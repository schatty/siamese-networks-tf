import setuptools
from setuptools import setup


setup(name='siamnet',
      version='0.0.1',
      license='MIT',
      packages=setuptools.find_packages(),
      install_requires=['Pillow', 'tqdm'])
